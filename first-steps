#!/usr/bin/env python3
# SPDX-License-Identifier: GPL-3.0-or-later
# Copyright 2026 First Steps Contributors
"""Entry point for the First Steps onboarding wizard."""

import sys
import os

# Ensure the package is importable when running from source
sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from first_steps.app import FirstStepsApp


def main() -> int:
    app = FirstStepsApp()
    return app.run(sys.argv)


if __name__ == "__main__":
    sys.exit(main())
